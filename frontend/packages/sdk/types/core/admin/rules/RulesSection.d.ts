import type { AdminSectionProps } from '../sections/registry';
export default function RulesSection({ params, navigate }: AdminSectionProps): import("react").JSX.Element;
/** The run log as its own place, so it is findable rather than buried. */
export declare function RulesLogSection({ params }: AdminSectionProps): import("react").JSX.Element;
